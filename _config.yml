# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: Chu's blog
email: chuw@andrew.cmu.edu
description: > # this means to ignore newlines until "baseurl:"
  this time – this space personal development and self improvement blog is focused on simple living and overcoming challenges by skillful means.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://curiouschild.github.io/" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: ChuWu1995
linked_in_username: Chu Wu
github_username:  Null
instagram_username:  Null
paginate: 5
paginate_path: "page:num"

# Build settings
markdown: kramdown
kramdown:
  syntax_highlighter: rouge
theme: minima
highlighter: rouge
plugins:
  - jekyll-feed

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
# exclude:
#   - Gemfile
#   - Gemfile.lock
#   - node_modules
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/

defaults:
  -
    scope:
      path: ""      # an empty string here means all files in the project
      type: "posts" # previously `post` in Jekyll 2.2.
    values:
      layout: "post"
      author: "Chu Wu"
